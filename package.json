{
  "name": "locoutils37",
  "version": "2.0.0",
  "private": true,
  "description": "Location d'outils à Tours — Netlify + Stripe + Supabase",
  "scripts": {
    "build": "node build-config.js",
    "dev": "netlify dev",
    "test": "playwright test",
    "test:syntax": "node scripts/check-project.js",
    "check": "npm run test:syntax && npm test"
  },
  "dependencies": {
    "stripe": "^18.4.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.2",
    "netlify-cli": "^23.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
const {json,sb}=require('./_shared');
exports.handler=async event=>{const origin=event.headers.origin||'';if(event.httpMethod==='OPTIONS')return json(200,{},origin);if(event.httpMethod!=='GET')return json(405,{error:'Méthode non autorisée.'},origin);try{const id=event.queryStringParameters?.session_id;if(!id)return json(400,{error:'Session absente.'},origin);const q=await sb(`reservations?stripe_session_id=eq.${encodeURIComponent(id)}&select=tool_name,duration,date_start,date_end,amount_location,amount_deposit,payment_status,deposit_status,reservation_status`);const r=q.data?.[0];if(!r)return json(404,{error:'Réservation introuvable.'},origin);const daysUntil=Math.ceil((Date.parse(`${r.date_start}T12:00:00Z`)-Date.now())/86400000);return json(200,{toolName:r.tool_name,duration:r.duration,dateStart:r.date_start,dateEnd:r.date_end,amountLocation:r.amount_location,amountDeposit:r.amount_deposit,paymentStatus:r.payment_status,depositStatus:r.deposit_status,reservationStatus:r.reservation_status,depositAuthorizationAvailable:daysUntil<=5},origin)}catch(e){console.error('get-reservation-status',e);return json(500,{error:'Statut momentanément indisponible.'},origin)}};
